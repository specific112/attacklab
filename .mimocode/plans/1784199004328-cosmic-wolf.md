# Plan: Add Videos link to admin header navigation

## Task
Add a "Videos" link to the admin dashboard header so the admin can quickly navigate to video management. The video management page (`/admin/videos`) is already admin-only protected via `requireAdmin()` — regular users cannot access it.

## File to modify
- `app/admin/page.tsx` — Add `<Link href="/admin/videos">Videos</Link>` to the header nav (line 112 area, next to Users and Courses links)

## What NOT to change
- The user dashboard (`app/dashboard/page.tsx`) — no video link needed here since users shouldn't see admin video management
- The video management page — already admin-only

## Verification
1. `npm run build` passes
2. Admin header shows: Users | Courses | Videos | View Courses | My Dashboard | Home
3. Regular users see no video management link anywhere
